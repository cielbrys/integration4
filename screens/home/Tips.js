import * as React from 'react';
import { StyleSheet, Text, View, SafeAreaView } from 'react-native';
import { useObserver } from 'mobx-react-lite';

export default () => {

  const tips = [
    'When camping, making a DIY bed in your car by putting your mattras in the back of the car is a top option, or using a tent outside is just as good. Do make sure when looking for a place to sleep at night, it is legal to sleep there.',
    'Try and take the smaller roads to make your experience as fresh as possible. Try to take brakes after lengthy periods of driving to calm the mind.',
    
  ]

  return (
    <Text>Hello</Text>
  );
};

const style = StyleSheet.create({

});
